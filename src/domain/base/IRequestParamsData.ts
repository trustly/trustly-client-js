import {IData} from './IData';

/**
 * Marker interface for restricting the generics through the project. We can use it to say that we want request params data, but not at all
 * of what type.
 */
export type IRequestParamsData = IData
